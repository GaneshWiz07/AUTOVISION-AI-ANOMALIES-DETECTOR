# Copy this file to .env and adjust for your setup.
# Not used in `npm run dev` (Vite proxies /api to the target in vite.config.js
# instead) - this only matters for production builds / preview / other hosts.

VITE_API_URL=http://localhost:12000/api/v1
